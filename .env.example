# Neon > Dashboard > Connect (usa la cadena "Pooled connection").
# Si creas la base desde Vercel > Storage > Neon, esta variable se agrega sola.
DATABASE_URL=postgresql://usuario:clave@ep-xxxx-pooler.sa-east-1.aws.neon.tech/neondb?sslmode=require
